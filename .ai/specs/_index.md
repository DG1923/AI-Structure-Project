# Specs Index

> All specs for this project. One row per spec. Never delete rows — mark as `done`.
> AI reads this at session start to understand what features exist and their state.

---

| Spec file | Feature name | Status | Date | Notes |
|-----------|-------------|--------|------|-------|
| — | — | — | — | No specs yet |

---

## Status values

| Status | Meaning |
|--------|---------|
| `draft` | Being written, not ready for review |
| `review` | Ready for human review |
| `approved` | Approved — implementation may begin |
| `in-progress` | Implementation started |
| `done` | Shipped and verified |
| `cancelled` | Will not implement — reason in Notes |

---

<!-- Add a row here for every new spec created -->
